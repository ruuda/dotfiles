[
  {
    "context": "Workspace",
    "bindings": {
      "\\ f": "file_finder::Toggle"
    }
  },
  {
    "context": "Editor && (vim_mode == normal || vim_mode == helix_normal)",
    "bindings": {
      // These bindings make things consistent with my Vim and Helix configs.
      "\\ w": "workspace::Save",
      "\\ x": ["workspace::SendKeystrokes", ": x"],
      "\\ r": "editor::Rewrap"
    }
  }
]
